{
  "name": "@kurone-kito/pnpm-workspace-template",
  "version": "0.1.0",
  "private": true,
  "description": "The pnpm workspace boilerplate project of the Node.js",
  "keywords": ["boilerplate", "pnpm", "template", "workspaces"],
  "homepage": "https://github.com/kurone-kito/pnpm-workspace-template#readme",
  "bugs": "https://github.com/kurone-kito/pnpm-workspace-template/issues",
  "repository": "github:kurone-kito/pnpm-workspace-template",
  "license": "MIT",
  "author": "kurone-kito <krone@kit.black> (https://kit.black/)",
  "type": "module",
  "files": [],
  "workspaces": {
    "packages": ["./packages/*"]
  },
  "scripts": {
    "build": "pnpm --aggregate-output --if-present --workspace-concurrency=0 -r run build",
    "build:sea": "pnpm --filter \"@kurone-kito/pwt-cli\" run build:sea",
    "cache": "sea-cache",
    "clean": "pnpm run --no-bail \"/^clean:.+/\"",
    "clean:root": "rimraf -g \"*.tgz\" \"*.tsbuildinfo\" coverage node_modules/.cache",
    "clean:sub": "pnpm --aggregate-output --if-present --parallel --workspace-concurrency=0 -r run clean",
    "cli": "pwt-cli",
    "dev": "pnpm --if-present --parallel --stream --workspace-concurrency=Infinity -r run dev",
    "lint": "pnpm run \"/lint:.+:check/\"",
    "lint:biome:check": "biome check",
    "lint:biome:fix": "biome check --write",
    "lint:cspell:check": "cspell lint --no-progress --show-suggestions -u \"./**/*\"",
    "lint:fix": "pnpm run \"/lint:.+:fix/\"",
    "lint:markdown:check": "markdownlint-cli2 \"**/*.md\"",
    "lint:markdown:fix": "markdownlint-cli2 --fix \"**/*.md\"",
    "pnpm:devPreinstall": "pnpm run -r devPreinstall",
    "postlint:fix": "pnpm run lint:cspell:check",
    "prebuild:sea": "pnpm --filter \"@kurone-kito/pwt-cli^...\" run build",
    "prepare": "husky",
    "predev": "pnpm --filter \"@kurone-kito/pwt-lib\" run build",
    "pretest": "pnpm --aggregate-output --if-present --workspace-concurrency=0 --filter \"@kurone-kito/pwt-*\" run build",
    "publish": "pnpm -r publish --no-git-checks",
    "publish:next": "pnpm -r publish --tag next --no-git-checks",
    "test": "pnpm run \"/test:.+/\"",
    "test:e2e": "pnpm run -r test:e2e",
    "test:ts": "pnpm --aggregate-output --workspace-concurrency=0 -r exec tsc -p tsconfig.test.json --noEmit",
    "test:vitest": "NODE_OPTIONS=\"--conditions=browser\" vitest run --coverage"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@cspell/cspell-types": "^9.1.3",
    "@kurone-kito/biome-config": "^0.20.0",
    "@kurone-kito/commitlint-config": "^0.20.0",
    "@kurone-kito/cspell-config": "^0.20.0",
    "@kurone-kito/lint-staged-config": "^0.20.0",
    "@kurone-kito/markdownlint-config": "^0.20.0",
    "@kurone-kito/pwt-cli": "workspace:^",
    "@vitest/coverage-v8": "^3.2.4",
    "cspell": "^9.1.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "markdownlint-cli2": "^0.18.1",
    "rimraf": "^6.0.1",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.12.4+sha512.5ea8b0deed94ed68691c9bad4c955492705c5eeb8a87ef86bc62c74a26b037b08ff9570f108b2e4dbd1dd1a9186fea925e527f141c648e85af45631074680184",
  "engines": {
    "node": "^20.11 || ^22 || >=24"
  },
  "publishConfig": {
    "access": "public"
  }
}
